apiVersion: apps/v1
kind: Deployment
metadata:
  name: notificator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notificator
  template:
    metadata:
      labels:
        app: notificator
    spec:
      topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: topology.kubernetes.io/zone
        whenUnsatisfiable: DoNotSchedule
        labelSelector:
          matchLabels:
            app: notificator
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
      serviceAccount: nsse-sa-prod
      volumes:
      - name: kestrel
        secret:
          secretName: kestrel-certificate
      imagePullSecrets:
      - name: ecr-image-pull-credentials
      containers:
      - name: notificator
        image: 968225077300.dkr.ecr.us-east-1.amazonaws.com/nsse/production/notificator
        ports:
        - containerPort: 4443
        envFrom:
        - configMapRef:
            name: config-map
        volumeMounts:
        - name: kestrel
          readOnly: true
          mountPath: /nsse-backend/certificates/kestrel
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "64Mi"
            cpu: "50m"
        livenessProbe:
          httpGet:
            path: /notificator/health
            port: 4443
            scheme: HTTPS
          successThreshold: 1
          failureThreshold: 3
          initialDelaySeconds: 120
          timeoutSeconds: 15
          periodSeconds: 60
        readinessProbe:
          httpGet:
            path: /notificator/health
            port: 4443
            scheme: HTTPS
          successThreshold: 1
          failureThreshold: 3
          initialDelaySeconds: 3
          timeoutSeconds: 15
          periodSeconds: 3